

## Changes

1. **Update headline text** in `src/components/HeroSection.tsx` (line 35): Change "Fueling patient outcomes" to "Fueling positive patient outcomes"

2. **Reduce font size** to fit on two lines: Change from `text-3xl sm:text-4xl md:text-5xl lg:text-[3.5rem]` to approximately `text-2xl sm:text-3xl md:text-4xl lg:text-[2.8rem]` — this smaller size should wrap the longer headline onto exactly two lines on desktop.

